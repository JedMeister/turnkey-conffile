#!/usr/bin/env python3

from distutils.core import setup

setup(name='turnkey-conffile',
    version='1.0',
    description='A minimalist config file parser',
    author='Stefan Davis',
    url='https://github.com/turnkeylinux/turnkey-conffile',
    packages=['conffile'])
